# .clang-tidy 파일에 사용

Checks: # 사용할 정적 분석 체크 목록
  - modernize-*
  - readability-*
  - bugprone-*
  - performance-*
  - cppcoreguidelines-*
  - -clang-analyzer-cplusplus.NewDeleteLeaks
  - -bugprone-easily-swappable-parameters # 이 설정은 CMake 설정의 -bugprone-easily-swappable-parameters와 중복되므로, .clang-tidy에 남겨둡니다.

WarningsAsErrors: "*" # 모든 경고를 오류로 처리 (CMake 설정과 중복되므로 .clang-tidy에만 남김)

HeaderFilterRegex: ".*" # 모든 헤더 파일을 검사 (CMake 설정과 중복되므로 .clang-tidy에만 남김)

FormatStyle: file

CheckOptions: # 각 체크별 세부 옵션 설정 (이 부분은 CMake에서 설정 불가능)
  - key: modernize-use-nullptr.NullMacros
    value: "NULL"

  # 함수 이름, 변수 이름, 파라미터는 스네이크 케이스
  - key: readability-identifier-naming.FunctionCase
    value: lower_case
  - key: readability-identifier-naming.VariableCase
    value: lower_case
  - key: readability-identifier-naming.ParameterCase
    value: lower_case
  - key: readability-identifier-naming.MemberCase
    value: lower_case

  # 클래스, 구조체, enum 등 타입명은 PascalCase
  - key: readability-identifier-naming.ClassCase
    value: CamelCase
  - key: readability-identifier-naming.StructCase
    value: CamelCase
  - key: readability-identifier-naming.EnumCase
    value: CamelCase

  # 상수는 모두 대문자 + 언더스코어
  - key: readability-identifier-naming.ConstantCase
    value: UPPER_CASE
  - key: readability-identifier-naming.EnumConstantCase
    value: UPPER_CASE